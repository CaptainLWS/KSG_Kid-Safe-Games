"use client";
import { FormEvent, useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [mode,setMode]=useState<"login"|"signup">("login");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const s=createClient();const r=mode==="login"?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password});if(r.error)setMessage(r.error.message);else if(mode==="signup")setMessage("Account created. Check your email if confirmation is enabled.");else location.href="/ship";}return <main className="shell"><div className="card" style={{maxWidth:520,margin:"80px auto"}}><p className="muted">SPACE LEAF IDENTITY</p><h1>{mode==="login"?"Welcome aboard":"Create your ecosystem identity"}</h1><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button">{mode==="login"?"Sign in":"Create account"}</button></form><p className="muted">{message}</p><button onClick={()=>setMode(mode==="login"?"signup":"login")} style={{background:"none",border:0,color:"#b7ffcf",cursor:"pointer"}}>{mode==="login"?"Need an account?":"Already have an account?"}</button></div></main>}
