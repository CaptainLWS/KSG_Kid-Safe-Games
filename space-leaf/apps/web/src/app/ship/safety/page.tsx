import { createClient } from "@/lib/supabase/server";
export default async function Safety(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return null;const {data}=await s.from("ecosystem_safety_policies").select("slug,name,version,rules,enabled,updated_at").eq("enabled",true).order("name");return <main className="shell"><div className="card"><p className="muted">SUNSHINE SHIELD</p><h1>Safety Policy Surface</h1><p>Client-visible policy metadata is read-only. Enforcement decisions remain server-side and are audited.</p></div><section className="grid" style={{marginTop:20}}>{(data||[]).map(p=><div className="card" key={p.slug}><h3>{p.name}</h3><p>Version {p.version}</p><pre style={{whiteSpace:"pre-wrap"}}>{JSON.stringify(p.rules,null,2)}</pre></div>)}</section></main>}
